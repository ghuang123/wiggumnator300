## 2024-05-28 - [Color formatting terminal outputs as UX improvements]
**Learning:** Terminal outputs lack affordances. Adding colors helps make UI more intuitive and creates feedback affordances for error, success, warning, etc, significantly improving readability. But remember to strictly obey constraints (e.g. 50 lines constraint from Palette prompt).
**Action:** Enhance readability by selectively colorizing terminal output but don't introduce overly massive change sets across too many files at once if under line constraint rules.
