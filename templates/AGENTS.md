# Operational Guide

<!-- This file is loaded at the start of every Wiggum loop iteration. -->
<!-- Keep it lean — operational knowledge only. No status updates. -->

## Build & Run

<!-- How to build and run the project. Fill in during scope definition. -->

```bash
# Build command:

# Run command:

```

## Validation

<!-- Run these after every implementation to get immediate feedback. -->

```bash
# Tests:

# Type check:

# Lint:

```

## Operational Notes

<!-- Learnings about how the project works that help future iterations. -->
<!-- Example: "The database connection requires FOO_DB_URL env var" -->

## Codebase Patterns

<!-- Key patterns and conventions used in this codebase. -->
<!-- Example: "All API handlers follow the pattern in src/handlers/example.ts" -->
